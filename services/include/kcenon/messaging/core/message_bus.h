#pragma once

namespace kcenon::messaging::core {
    class message_bus {
    public:
        message_bus() = default;
        ~message_bus() = default;

        // Placeholder implementation for Phase 1
        void initialize() {}
        void shutdown() {}
    };
}