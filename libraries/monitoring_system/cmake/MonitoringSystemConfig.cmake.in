@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/MonitoringSystemTargets.cmake")

check_required_components(MonitoringSystem)

# Find dependencies
find_package(Threads REQUIRED)

# Optional: Provide version information
set(MonitoringSystem_VERSION @PROJECT_VERSION@)