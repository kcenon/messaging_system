@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/ThreadSystemCoreTargets.cmake")

check_required_components(ThreadSystemCore)

# Find dependencies
include(CMakeFindDependencyMacro)
find_dependency(Threads)
EOF < /dev/null